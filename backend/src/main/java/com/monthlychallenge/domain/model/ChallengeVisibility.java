package com.monthlychallenge.domain.model;

public enum ChallengeVisibility {
    /** Visible to accepted friends (default). */
    SHARED,
    /** Hidden from everyone including friends. */
    PRIVATE
}
